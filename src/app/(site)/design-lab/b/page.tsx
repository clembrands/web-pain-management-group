import { HomeNarrative } from "@/components/home/narrative";

// Direction B, chosen as the site's design system. This page is now the same as Home and
// goes away with the rest of the design lab once the interior template is done.
export default function DirectionB() {
  return <HomeNarrative />;
}
